println("Hello, World!");

thing = (a: num, b: str, c: bool) -> {
    another = (d: str, e: num) -> {
        println(d);
    };

    another(b, 2);

    println("Test Function");
    println(b);
};

thing(1.2, "hello", false);